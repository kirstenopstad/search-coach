@{
  Layout = "_Layout";
}

@using SearchCoach.Models;

<h1>Welcome to the Search Coach!</h1>

<hr>

<div class="jumbotron text-center">
  <h1>Hi</h1>

  <div class="card mb-4 pt-2">
    <h4>Applications</h4>
    @if (Model["applications"].Length == 0)
    {
      <p>No applications have been added yet!</p>
    } 
    <ul class="list-group">
      @foreach (Company company in Model["applications"])
      {
        <li class="list-group-item list-group-item-action">@Html.ActionLink(@applications.Role, "Details", "Applications", new { id = @applications.ApplicationId}) | </li>
      }
    </ul>
  </div>
  <!-- <div class="card pt-2">
    <h4>Applications</h4>
    @if (Model["applications"].Length == 0)
    {
      <p>No applications have been added yet!</p>
    } 
    <ul class="list-group">
      @foreach (Flavor flavor in Model["flavors"])
      {
        <li class="list-group-item list-group-item-action">@Html.ActionLink(@flavor.Name, "Details", "Flavors", new { id = @flavor.FlavorId})</li>
      }
    </ul>
  </div> -->


</div>
<hr>

<p class="enlarge">@Html.ActionLink("Manage treats", "Index", "Treats")</p> 
<p class="enlarge">@Html.ActionLink("Manage flavors", "Index", "Flavors")</p>
<p class="enlarge">@Html.ActionLink("Create or manage an account", "Index", "Account")</p>